# Templates

The reference implementation currently renders SVG directly from Python for maximum determinism and minimal runtime dependencies. This directory is reserved for organizations that want to swap in Jinja2 SVG/HTML templates without changing the semantic model/layout pipeline.
